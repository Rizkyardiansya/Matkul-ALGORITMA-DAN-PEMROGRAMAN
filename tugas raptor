#include <iostream>
#include <string>

using namespace std;
int main()
{
   string raptor_prompt_variable_zzyz;
   ?? x;
   ?? y;
   ?? matriks;
   ??[,] b = new ??[??+1,??+1];
   ??[,] c = new ??[??+1,??+1];
   ??[,] a = new ??[??+1,??+1];

   raptor_prompt_variable_zzyz ="Ketik '1' untuk perkalian matriks silang dan ketik '2' untuk perkalian matriks titik";
   cout << raptor_prompt_variable_zzyz << endl;
   cin >> matriks;
   if (matriks==1)
   {
      x =0;
      y =0;
      while (1)
      {
         raptor_prompt_variable_zzyz ="masukkan angka untuk matriks A baris "+(x+1)+" kolom "+(y+1);
         cout << raptor_prompt_variable_zzyz << endl;
         cin >> a(x+1,y+1);
         raptor_prompt_variable_zzyz ="masukkan angka untuk matriks A baris "+(x+1)+" kolom "+(y+2);
         cout << raptor_prompt_variable_zzyz << endl;
         cin >> a(x+1,y+2);
         x =x+1;
         y =y+2;
         if (x==2 && y==2)) break;
         y =0;
      }
      x =0;
      y =0;
      while (1)
      {
         raptor_prompt_variable_zzyz ="masukkan angka untuk matriks B baris "+(x+1)+" kolom "+(y+1);
         cout << raptor_prompt_variable_zzyz << endl;
         cin >> b(x+1,y+1);
         raptor_prompt_variable_zzyz ="masukkan angka untuk matriks B baris "+(x+1)+" kolom "+(y+2);
         cout << raptor_prompt_variable_zzyz << endl;
         cin >> b(x+1,y+2);
         x =x+1;
         y =y+2;
         if (x==2 && y==2)) break;
         y =0;
      }
      cout << "Matriks A = " << endl;      cout << "[ "+a(1,1)+" "+a(1,2)+" ]" << endl;      cout << "[ "+a(2,1)+" "+a(2,2)+" ]" << endl;      cout << "Matriks B = " << endl;      cout << "[ "+b(1,1)+" "+b(1,2)+" ]" << endl;      cout << "[ "+b(2,1)+" "+b(2,2)+" ]" << endl;      cout << "Matriks C = Matriks A x Matriks B" << endl;      x =1;
      y =1;
      while (1)
      {
         if (x==1 && y==1)
         {
            C[1,1] = a(1,1)*b(1,1)+a(1,2)*b(2,1);
            cout << "C[1,1] ="+a(1,1)+" x "+b(1,1)+" + "+a(1,2)+" x "+b(2,1) << endl;            y =y+1;
         }
         else
         {
            if (x==1 && y==2)
            {
               C[1,2] = a(1,1)*b(1,2)+a(1,2)*b(2,2);
               cout << "C[1,2] ="+a(1,1)+" x "+b(1,2)+" + "+a(1,2)+" x "+b(2,2) << endl;               x =x+1;
               y =y-1;
            }
            else
            {
               if (x==2 && y==1)
               {
                  C[2,1] = a(2,1)*b(1,1)+a(2,2)*b(2,1);
                  cout << "C[2,1] ="+a(2,1)+" x "+b(1,1)+" + "+a(2,2)+" x "+b(2,1) << endl;                  y =y+1;
               }
               else
               {
                  if (x==2 && y==2)
                  {
                     C[2,2] = a(2,1)*b(1,2)+a(2,2)*b(2,2);
                     cout << "C[2,2] ="+a(2,1)+" x "+b(1,2)+" + "+a(2,2)+" x "+b(2,2) << endl;                     x =x+1;
                     y =y+1;
                  }
                  else
                  {
                  }
               }
            }
         }
         if (x==3 && y==3)) break;
      }
      cout << "Jadi, Matriks C =" << endl;      cout << "[ "+c(1,1)+" "+c(1,2)+" ]" << endl;      cout << "[ "+c(2,1)+" "+c(2,2)+" ]" << endl;   }
   else
   {
   }

   return 0;
}
